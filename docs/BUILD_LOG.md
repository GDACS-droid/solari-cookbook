# Build log

## 2026-09-01 — Cape Coral daily event-stream foundation

- **Source expansion:** verified official ArcGIS schemas and privacy projections for Code Enforcement, Utility Liens, Building Permits, and Payoff Data. Inspections remains review-required because a stable revision identity is unproven; 311 remains review-required after an ordinary filtered query returned 403 and because it is address-only/free-text sensitive.
- **Temporal engine:** added durable per-source snapshots, canonical state hashes, advancing end watermarks with bounded lookback, bootstrap suppression, stable transition IDs, delta omission preservation, and explicit source-event/source-update/first-seen/retrieval clocks.
- **Failure isolation:** strict response-schema hashes, stable bounded pagination, generated per-source snapshot budgets, partial-page discard, compare-and-swap generations, and independent source commits. A failed source never advances its watermark or destroys a healthy source run.
- **Privacy:** Code owner/mailing/free text/updater, Lien account/customer/name/confidentiality/address, Permit contractor/company, and Payoff `NAME` never enter the requested projection. Payoff is watchlist-only and never mislabeled as lien/tax distress.
- **Durability:** added an atomic restrictive local/VM JSON store and a production Postgres migration for runs, snapshots, privacy-minimal items, and idempotent transitions. Vercel scheduling remains disabled until an approved transactional database is provisioned.
- **Real official-data proof:** the opt-in live suite retrieved the Aug. 31 City code-event window, retrieved the selected exact parcel's Payoff state without `NAME`, and established a real three-source Code/Lien/Permit baseline. All three reloaded at generation 1 and bootstrap emitted zero `NEW_*` transitions.
- **Prepared commercial track:** documented seven public pilot targets, a truthful $750–$1,500/month concierge hypothesis, discovery questions, and exact outreach copy without sending it. Prepared the seven-question Lee Clerk bulk-data inquiry without signing, buying, or contacting externally.
- **Adversarial repairs:** an independent reviewer found that a 24-hour delta baseline could call a previously unseen old row “new,” generic lien amount/status changes were not observable from the source's dated query, permit value corrections could masquerade as status changes, observation clocks were ambiguous, transient source failures lacked retry, and the local store had lock/dedup edge cases. Added source-window classification, explicit clock basis, truthful supported-event scope, `PERMIT_UPDATED`, policy-bounded backoff, same-commit dedupe, and token/process-start lock fencing. The reviewer accepted the corrected event semantics; the local-store fence was tightened again after its follow-up found a slow-writer race in a time-only lease.
- **E2E environment repair:** the first browser regression run loaded an unrelated Bill Commons dev server already on port 3000 because Playwright allowed server reuse. AcreBrief E2E now owns port 3107 with reuse disabled; all 10 desktop Chromium/mobile WebKit checks pass against the intended app.
- **Verification:** 57 non-live tests pass with three official-source live checks opt-in; the live suite passed all three against Cape Coral, including the durable three-source baseline. Production build, build-trace assertion, 10 E2E tests, lint, TypeScript, source-policy drift, diff whitespace, and high-severity dependency audit passed. Final desktop/mobile production-build captures preserve the AcreBrief brand and render the founding-pilot price without overflow.

## 2026-09-01 — evidence-first foundation

- **Observed:** local repository remote configuration identifies `GDACS-droid/solari-cookbook` as a fork of `solari-sdk/solari-cookbook`. Upstream examples demonstrate Browser profiles/recording, Sandbox execution/preview, and Desktop computer-use.
- **Observed:** Lee Clerk advertises official-record search, court inquiry, weekly foreclosure registry/sale resources, and paid bulk options; Lee PA supports STRAP/folio/address/recording lookup and GeoView. Collier and Charlotte entry points were catalogued with source-specific uncertainty.
- **Decision:** use Lee as the real vertical slice; model Charlotte/Collier as researched but gated expansion sources.
- **Implemented documentation:** source registry, Solari design, source matrix, product/competition framing, privacy/security controls, unit-economics instrumentation, demo and launch draft.
- **Implemented vertical slice:** canonical property/event/evidence model, stable fingerprints, snapshot diffs, explainable scoring, candidate-resolution queue, partial adapter failure handling, SSE investigation stream, privacy-safe recorded portal checks, unrecorded property-marker verification, and isolated Solari Sandbox manifest validation.
- **Corrected during integration:** the verified fixture is in Lehigh Acres, not Cape Coral. The May notice-of-action and August foreclosure-sale notice are distinct evidence artifacts; neither is represented as an official live docket response.
- **Verified locally after adversarial fixes:** clean `npm ci`; generated-policy drift check; lint; TypeScript; 19 unit/API/policy tests; production build; 6 Playwright E2E checks across desktop Chrome and mobile Safari; 20-source registry schema validation; dependency audit with zero known vulnerabilities; desktop/mobile full-page visual inspection; and a 72-second recorded walkthrough.
- **Solari runtime proof:** a bounded real-key smoke launched Solari Browser, loaded `https://example.com`, verified the page title, released the session, created a Solari Sandbox, ran a deterministic Node command, verified stdout, and killed the sandbox. No gated property source was opened and no session/profile identifier or credential was printed.
- **Known boundary:** desk research does not authorize unattended automation. Production property adapters remain gated until source-specific terms/access and rate limits are approved. The SDK/account path is proven; a source-specific live property run is intentionally still blocked by the empty approval policy.
- **Adversarial fixes:** replaced the incompatible CommonJS SDK loader with ESM imports plus Next server externalization; token-gated and cancelable paid runs; added one-run concurrency; enforced a default-deny source policy; required fresh property evidence before a live graph; separated unresolved candidate IDs from parcel IDs; added source-native event fingerprints; fixed fixture score time; and removed illustrative dashboard/operations counts that could be mistaken for observed data.
- **Public preview proof:** deployed the credential-free verified-sample build to Vercel, assigned `acrebrief-preview.vercel.app`, confirmed HTTP 200 and security headers, replayed the score-46 brief to 4/4 completion, confirmed live mode fails closed with 412 and an unconfigured pilot sink returns 503, and rendered the hosted result in clean desktop Chromium and mobile WebKit sessions without overflow.
- **Post-key adversarial hardening:** rebound live events to same-run evidence; replaced fixture-derived live graph assembly with source-native normalized events; generated runtime authorization from schema-validated YAML policy metadata with a CI drift check; enforced exact URLs, approval expiry, reviewer, terms-review date, and per-run request budgets; rejected redirected origins/paths; disabled recording pending retention controls; retained only named marker booleans and a content hash; and introduced a distinct low-confidence `review_required` terminal result for non-official publication checks. Publisher reconnaissance found `/news/` is not robots-disallowed but found no affirmative automation/rate authorization, so the exact property sources remain disabled.

## 2026-09-01 — LIVE_READY official-data slice

- **Source authorization solved by source mix:** Florida DOR states its current NAL/SDF/GIS files are publicly downloadable and exclude confidential/exempt records. Lee PA expressly offers free data. Cape Coral operates a City Open Data ArcGIS service with parcel-keyed utility-lien rows. Added `access_basis` to every registry source and made generated policy reject approved `REVIEW_REQUIRED` entries.
- **Implemented real vertical:** Solari Browser verifies the DOR catalog; Solari Sandbox downloads the live 43,316,780-byte Lee `2026P` NAL ZIP, validates and expands the 285,742,514-byte CSV, parses the 165-column schema, discards owner/mailing fields, and emits the exact parcel `174423C3039260170`. One privacy-projected City Open Data row is fetched and the same Sandbox verifies its STRAP exactly equals the DOR parcel ID.
- **Real failure and root-cause repair:** first live attempt failed safely because the Solari base Sandbox has no `unzip` binary. Replaced that assumption with a standard-library ZIP validator enforcing one exact entry, no traversal/encryption, 60 MB compressed and 400 MB expanded ceilings. The next real run completed end-to-end in about 46 seconds.
- **Privacy hardening:** no City customer/account/name/service-address fields are requested; no DOR owner/mailing fields leave the Sandbox. Raw provider Browser/Sandbox handles are replaced with one-way 12-character run references before SSE output.
- **Honest event semantics:** the current City row is source-reported active but dated February 2022; the table has no 2026 active event. Score is 10, with zero fake recency/equity points. Fresh court/foreclosure/tax/title facts stay unavailable.
- **Verification after implementation:** source-policy check, lint, TypeScript, 23 tests, production build, 6 desktop/mobile E2E tests, full-page desktop/mobile inspection, and a real current-key Solari Browser + Sandbox run all passed.
- **Adversarial repair and second real proof:** independent reviewers found that the historic City row was typed `NEW_LIEN`, the DOR parser accepted first-match/line-oriented CSV, direct reads lacked bounded retries, curl followed redirects, and client cancellation did not actively terminate remote work. Replaced the event with non-new `LIEN_STATUS_ACTIVE`; moved to a strict RFC-4180 parser with unique required headers, full-schema hash, and exact-one-row enforcement; added bounded retries/deadlines, no-redirect DOR retrieval, and abort-triggered Browser/Sandbox cleanup. Added malformed-header, duplicate-row, embedded-newline, no-fresh-status-bonus, and pre-cancel tests. The strengthened path then completed another real DOR→City run in 48 seconds with 2 evidence records, 0 owners, score 10, and hashed public run references. Full verification now passes 31 tests.
- **Final policy-race repair:** reviewers found that opaque curl retries could exceed the declared source budgets and cancellation could land while a remote create was still awaiting. Retries are now explicit physical attempts enforced by generated budgets (DOR 4, City 2), route setup handles an already-aborted request, and Browser/Sandbox creation rechecks cancellation and destroys a just-created resource before any source navigation or command. Budget-exhaustion and route pre-cancel regressions are covered.
- **Production edge protection:** created a clean Vercel Next.js project after the original persisted `Other/public` configuration rejected Next output, then published a WAF rule for `POST /api/investigations`: fixed window, 1 request/60 seconds, IP+JA4 keys, deny on exceed. The app-level lock/cooldown remains defense in depth; Vercel's documented regional counters are not represented as an atomic global quota.
- **Hosted-runtime incident and repair:** the first custom-domain live check returned HTTP 500 before the SSE stream began, while the same production route still replayed the verified sample. Runtime logs identified a missing computed-path `patchright-core/browsers.json` manifest after Next externalized the Solari/Patchright packages. Kept only Patchright's optional-module bundle external, bundled both Solari clients, explicitly included the Patchright manifest in the investigation function trace, and added a post-build assertion for both invariants. Solari initialization is also inside a fail-closed boundary so a future packaging regression produces an explicit failed terminal event rather than an opaque invocation error.

## 2026-09-01 — source-dated fresh event

- **Fresh official source:** the City of Cape Coral Code Enforcement Open Data table returned five `FORECLOSURE REGISTRATION` records source-opened August 31. The selected municipal case `CODE26-020878` carries stable `CMCODECASEID`, `opened`, `updated`, exact STRAP and site address. The City says the program applies when a mortgagee has initiated foreclosure and the property is vacant.
- **Honest semantics:** added `FORECLOSURE_REGISTRATION_OPENED`, not `NEW_FORECLOSURE_CASE`. Event/source-update/first-seen/retrieval clocks remain separate. A review then caught that relative “NEW YESTERDAY” copy would age incorrectly and repeat investigations rewrote first-seen time. Replaced the badge with an absolute source date, persisted the reviewed first-seen observation, moved retrieval time after the validated City response, and reserved `NEW_FORECLOSURE_REGISTRATION` for a durable snapshot diff.
- **Live Solari proof:** first new-path run failed closed on an incorrect STRAP-format validator; second reached DOR and failed closed when an ArcGIS hash-valued OID changed. Corrected the STRAP grammar and removed unstable `ESRI_OID` from identity. The third real run completed: Solari Browser verified DOR, Solari Sandbox parsed parcel `304424C2007000560`, the City API returned the registration, Sandbox verified exact STRAP→DOR equality, and AcreBrief emitted the evidence-backed graph.
- **Privacy:** the new City projection excludes owner, mailing, contact, and free-text description fields. DOR owner/mailing columns still never leave Sandbox.
- **UI precision:** preserved brand/logo and editorial brief; exposed four clocks, fixed misleading freshness wording, made evidence/operations mobile-readable, and limited motion changes to pointer-gated 150 ms button feedback.
- **Competition path:** the queue's primary action now starts the real investigation, focuses and lands on truthful Browser/Sandbox/direct-source progress, and renders the evidence-backed result. A production-build recording of that exact path is 60.64 seconds.
- **Cold visual review:** an independent reviewer correctly identified the product, primary source-dated signal, and intended action without coaching. Their material findings—ambiguous queue CTA, underqualified confidence, compressed mobile labels, and a development-only toolbar—were fixed and recaptured from the production build.
- **Final temporal-language audit:** replaced remaining static “latest,” present-tense “open,” and “new events” copy with an absolute source-date, `Open at Sep 1 retrieval`, and `Observed result`. The graph now also rejects a persisted first-seen observation whose source record ID does not match the retrieved City row.
- **Per-source retrieval clocks:** DOR evidence now records the time its validated parcel projection completed, while City evidence records its own post-parse retrieval time. The legacy lien compatibility path also rejects a mismatched persisted observation ID. A regression test proves the two evidence timestamps remain distinct.
- **Final verification:** 35 unit/API/policy tests, 10 Playwright checks across desktop Chromium and mobile WebKit, production build-trace validation, clean production-build desktop/mobile screenshots, a high-severity dependency audit with zero known vulnerabilities, and representative-frame inspection of the 60.64-second live recording all passed.
- **Release boundary:** the pre-release production baseline is Vercel deployment `dpl_FUUWG2oBfMC5nyG9HU3gTn6QPdYk`. It is the rollback target if the custom domain, readiness endpoint, temporal semantics, privacy projection, or one bounded live investigation fails after cutover. This release has no schema migration or durable write path; an alias rollback has no data-recovery step but can interrupt an in-flight SSE investigation.
- **Pre-cutover packaging failure:** the first release attempt failed in Vercel's build before alias movement because `.vercelignore` excluded the new runtime observation JSON under `data/`. The production baseline remained untouched. Unignored only `data/event_observations/official_demo_observations.json`; the broader research/data tree remains excluded from deployment.
- **Packaging root fix:** Vercel's uploader did not traverse the negated child of an excluded `data/` parent, so a second build also failed before cutover. Moved the bounded runtime observation artifact to `src/data/event_observations/`; restored the broad `data/` exclusion rather than weakening deployment packaging.
- **Ignore-matcher correction:** the unanchored `data/` deployment-ignore pattern also matched nested `src/data/`, so the next remote build again failed before cutover. Moved the single artifact to the explicit non-ignored path `src/runtime/official_demo_observations.json`; no deployment ignore exception remains.
