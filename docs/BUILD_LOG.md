# Build log

## 2026-09-01 — evidence-first foundation

- **Observed:** local repository remote configuration identifies `GDACS-droid/solari-cookbook` as a fork of `solari-sdk/solari-cookbook`. Upstream examples demonstrate Browser profiles/recording, Sandbox execution/preview, and Desktop computer-use.
- **Observed:** Lee Clerk advertises official-record search, court inquiry, weekly foreclosure registry/sale resources, and paid bulk options; Lee PA supports STRAP/folio/address/recording lookup and GeoView. Collier and Charlotte entry points were catalogued with source-specific uncertainty.
- **Decision:** use Lee as the real vertical slice; model Charlotte/Collier as researched but gated expansion sources.
- **Implemented documentation:** source registry, Solari design, source matrix, product/competition framing, privacy/security controls, unit-economics instrumentation, demo and launch draft.
- **Implemented vertical slice:** canonical property/event/evidence model, stable fingerprints, snapshot diffs, explainable scoring, candidate-resolution queue, partial adapter failure handling, SSE investigation stream, privacy-safe recorded portal checks, unrecorded property-marker verification, and isolated Solari Sandbox manifest validation.
- **Corrected during integration:** the verified fixture is in Lehigh Acres, not Cape Coral. The May notice-of-action and August foreclosure-sale notice are distinct evidence artifacts; neither is represented as an official live docket response.
- **Verified locally after adversarial fixes:** clean `npm ci`; lint; TypeScript; 14 unit/API tests; production build; 6 Playwright E2E checks across desktop Chrome and mobile Safari; 20-source registry schema validation; dependency audit with zero known vulnerabilities; desktop/mobile full-page visual inspection; and a 72-second recorded walkthrough.
- **Solari runtime proof:** a bounded real-key smoke launched Solari Browser, loaded `https://example.com`, verified the page title, released the session, created a Solari Sandbox, ran a deterministic Node command, verified stdout, and killed the sandbox. No gated property source was opened and no session/profile identifier or credential was printed.
- **Known boundary:** desk research does not authorize unattended automation. Production property adapters remain gated until source-specific terms/access and rate limits are approved. The SDK/account path is proven; a source-specific live property run is intentionally still blocked by the empty approval policy.
- **Adversarial fixes:** replaced the incompatible CommonJS SDK loader with ESM imports plus Next server externalization; token-gated and cancelable paid runs; added one-run concurrency; enforced a default-deny source policy; required fresh property evidence before a live graph; separated unresolved candidate IDs from parcel IDs; added source-native event fingerprints; fixed fixture score time; and removed illustrative dashboard/operations counts that could be mistaken for observed data.
- **Public preview proof:** deployed the credential-free verified-sample build to Vercel, assigned `acrebrief-preview.vercel.app`, confirmed HTTP 200 and security headers, replayed the score-46 brief to 4/4 completion, confirmed live mode fails closed with 412 and an unconfigured pilot sink returns 503, and rendered the hosted result in clean desktop Chromium and mobile WebKit sessions without overflow.
